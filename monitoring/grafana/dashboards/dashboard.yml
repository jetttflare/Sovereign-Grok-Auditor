# GrokApp Grafana Dashboard
# =========================

apiVersion: 1

providers:
  - name: 'GrokApp'
    orgId: 1
    folder: 'GrokApp'
    folderUid: 'grokapp'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
